# Student Grade Management System

Branch project based on the Spring 2026 OOP project collection.

## Implementation

Implements student objects, dynamic grades, GPA/standing classification, sorting, delete/search, and file persistence.

## Build

```bash
cmake -S . -B build
cmake --build build
```

Data files are created in the process working directory when persistence/logging is used.
